DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/tafsir_vocab?sslmode=require"

# Quran Foundation Content API — keep these server-side only.
# New apps normally start in prelive; switch to production after approval.
QF_ENV="prelive"
QF_CLIENT_ID=""
QF_CLIENT_SECRET=""
